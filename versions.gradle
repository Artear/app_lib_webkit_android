buildscript {
    ext {

        // -------- Library Version ---------
        libraryVersion = "0.0.4"

        // -------- Base Libraries ----------
        //Kotlin
        kotlin_version = '1.3.10'

        //Test

        espressoVersion = "3.0.2"
        espressoRunnerVersion = "1.0.2"
        mockitoVersion = "2.10.0"


        moshiVersion = "1.7.0"
        okio = "1.15.0"

        // -- Support
        supportLibraryVersion = "28.0.0"
        // -- Logger
        timberVersion = "4.7.1"

        // -- Rx
        rxJavaVersion = "2.1.0"
        rxAndroidVersion = "2.0.1"

        // ------------- Artear -------------
        // -- Image Handling
        thumbnailkitVersion = "0.2.4"
        // -- Tracking
        multiTrackerVersion = "0.1.1"
        // -- Tools
        toolsVersion = "0.0.13"
        // -- UI
        uiViewVersion = "0.1.5"
        // -- Injector
        injectorVersion = "0.0.3"
        // -- Domain
        domainVersion = "0.0.3"

    }
}